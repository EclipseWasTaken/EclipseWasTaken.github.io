// Smooth scroll disabled — native scrolling pairs cleanly with GSAP ScrollTrigger
// and avoids pin/refresh edge cases. CSS `scroll-behavior: smooth` is set in styles.
export function SmoothScroll() {
  return null;
}
